# TODO

- Nice to have:
  - Support one-off Docker image override in quick-launch mode with no explicit `run` subcommand.
  - After a successful login/import flow, offer or perform automatic profile save/bind so the authenticated account can be reused directly.
  - Later, support a Docker launch mode that keeps the container/image-backed temporary environment alive instead of cleaning it up immediately after exit.
