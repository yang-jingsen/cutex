# Private Job presentation: composed verification

**PARTIAL / BLOCKED: same-process reviewed restart did not complete during the generation-race proof.** Default display path and two process-death recoveries passed. Do not accept this as complete integration or deployment. No paid model, real credentials or existing Human fixture changes.

## Composition and compatibility

Component checkpoint `bcf40135df8b5c535a971dfa49ab3b5821fdeeeb` follows exact clean base `6ba94a592ac8b1f29083b6e1bab55b071c8754dd`. Compiled Rust is `5414db97f82f7615d6605925cca282dcccdf75d0`, tree `c6e7b45549d9cea45cab85a9507e2658bdc26c84`; subsequent changes are test evidence/docs only. Product ancestor remains `d89a1585bb9d2967a1078fa95019c8023af81297`.

The coherent accepted native source is `3d8a73a747cf5b957a7ca0491c28d1517f6d7722`, tree `0b10516b22741c640a58ab7faa7e49f95f1d98a8`. Native manifest SHA256 `28cbc9a64460db504fe842b787695144e14a90b7f939b7782dd0133d6e45d54c`; aggregate schema `77e75b7fc47c9b8a9caacf5a7d31c040c6520679a9833f1c2b0e55937ed39c27` (distinct from the schema-file hash-map hash). Exact CLI/server/host and both Cutex build variants are frozen in `../artifacts/presentation-job-r1/build-manifest.json`, SHA256 `0d67d1e62c694e89da72891303314970df0ed9dd4c8c0999e54c3799f61fdba1`.

Default Cutex SHA256 `03025fcd450cf1edb911cbfc937e23154f86523fa19f0f2171a4909a03bdc969`; default facade `54f705eace59ab51dd75566561544e6b499c058103559b56a23ca7082c30856b`. Fault-injection feature binaries are separately identified, never called a default distribution. Builds used Rust1.95/locked dev profile and owned Mambo cache; no dependency or native rebuild.

New reviews accept only this exact current v3 family, including presentationVersion1 + ExternalInput1/Soon. Previous ca580/mixed components reject; old markers and immutable receipts are not rewritten. Earlier separately supported stock/ingress families and unmarked K behavior are unchanged. Official stock remains registration-only, with no presentation fallback. Bus v5 is created only when an explicitly configured new Job event carries an obligation. Old writers must not open this private store; reject/revert before exposure, not downgrade/history rewrite.

## Consumer contract and actual display

Configuration is default absent: `private_job_presentation: {"version":1,"recipients":["<exact canonical durable ID>"]}` in existing checked local service config, max32 recipients. This is not a Job wire option, environment authorization, model tool or general offline display API. Only the existing dedicated authenticated Job completion route creates an obligation. Replay/config changes do not backfill old messages; input-only/ordinary MCP produces no additional presentation.

Canonical acceptance atomically freezes version1 template intent and source. Full native target/digest freezes only after validating the original persisted input A4 reference. Display persistence has its own ID/digest/receipt. Input A4 → canonical delivered → Job ACK does not wait for display; errors/status-null/replay are not interchanged. No held input is released, no session is launched merely for display. Existing canonical records are retained; no pruning or new ledger was added.

Actual default-byte PTY showed a separate native Notice, with an explicit linked supplement (not a merged card):

```text
Notice · service/cutex-job-service
Job 终态通知
Related external input: <actual canonical input ID> (linked supplement)
Job: private-job
状态：exited
输出读取状态：未观测。
摘要（外部数据）：SERVICE_DISPLAY_BODY
```

`exited` does not assert exit0. Output-read status is not observed by this API. The Chinese display text and independent idle sentinel are absent from actual captured fake-provider requests. The ordinary input body remains the existing English Job completion template. Display receipt is neither A4, business execution, user-seen proof nor Job success.

## Executable evidence

Private fixtures use user/PID/network namespaces (only loopback), owned-port pre-connect tripwire, fake credentials and local fake Responses. The producer posts to the real dedicated Cutex Job-completion handler using its actual separate service token; a normal Bus token is refused. This is a real Cutex/native consumer path with a fixture producer, **not a new Job daemon/subprocess/grant/MCP execution proof**. Prior unchanged Job evidence is reused only at that named boundary.

| Gate | Evidence/status |
|---|---|
| Default bytes: root review/activation/replay, owned runtime generation1, stale controller refusal | `pj03/PASS.json`, passed |
| Standalone append/status/replay, zero turns/model calls before Job input | pj03 passed |
| Real input A4 + independent display receipt/timeline; default-off new event; exact replay | pj03 passed |
| Real same-owner PTY, idle/linked display and normal exit/termios restoration | pj03 passed |
| Independent persisted history: two display records, original A4 exactly once, display-only text excluded | `tests/presentation_history.py pj02`, `pj03`, `pj04` passed |
| After native append/before local CAS process death and recovery | pj04 passed: generation2, original display receipt, one fake model request, one input Commit |
| After Job ACK/before native display append process death and recovery | pj05 passed: generation2, original frozen ID/receipt, independent history check passed |
| Current-generation change before local display CAS | pj07 partial: real old-side refusal `lifecycle transition in progress`, no local display receipt; root restart timed out in Prepared at generation1, so subsequent generation2 convergence UNPROVEN |

Focused default Rust: presentation selection17 + store selection8 + new-pin selection6 =31 executions/28 distinct tests (three overlaps). Tests include native independent hash vector, UTF8/unknown/version/source/receipt validation, null-vs-missing, no-backfill/default-off, v5/old-reader predicate rejection, frozen replay/conflict and bounded record rotation. Store A4 unit fixtures are synthetic, distinct from the composed native proof. Existing checks (`--bins --lib`, `--tests --bins`) passed; final default and feature builds passed. No full workspace/all-features/Windows/Android or real-provider/production acceptance.

## Preserved failures and limits

- pj01: fixture terminated a neutral creator before its supported persistence acknowledgment; review failed with ENOENT. Corrected to exact accepted `thread/read(includeTurns=true)` on the loaded paginated thread. This native method publishes writer/metadata before success; it is not an incidental roundtrip or sleep. No duplicate retry of the uncertain pj01 identity.
- pj02: full display/input/history gates succeeded, but `/exit` plus Enter in one PTY burst remained pasted text; exit timed out. Corrected bracketed paste → observed composer text → separate Enter. pj03 passed, original evidence retained.
- pj06: real reviewed restart had not published its execution receipt within the test gate's30s budget. The gate timed out; no generation-race success was claimed. `e766ef4bd38499fe211da069845c561c6c4a3c4f` extends only the existing feature-gated post-presentation test latch to240s and bounded receipt observation to180s. Default production code/RPC/backoff is unchanged; default bytes remain the tested frozen5414 build.
- pj07: after actual input A4/Job delivered and native display append, a real root reviewed restart published `prepared`. Releasing the display latch produced the observed pre-CAS error `lifecycle transition in progress`; local receipt remained absent (`pj07/observed-precas-refusal.json`, read-only observation). The restart then exceeded the240s HTTP boundary with receipt still Prepared/generation1. This is not claimed as successful stop, recovery, rollback or a confirmed deadlock cause. The fixture was terminated within its private PID namespace; the potentially useful persisted records remain. No further whole-path retry was run.
- The fake fixture emits the existing sandbox user-namespace advisory and unknown-model metadata warning. No command sandbox/path-denial claim is made here; unchanged native sandbox proofs are reused, not rerun or bypassed.
- Display recovery remains synchronous in the existing worker, max4 due records, 30s backoff and existing bounded RPC timeouts. Current input ACK is already complete, but later polls can be delayed by display checks. No throughput/hostile-same-UID or power-loss durability claim.
- Human pi1/hj1/r3/r4, production auth/services/PRH and native/Job sources remain untouched. Private test children only are stopped; prior intentionally running Human fixtures are not cleaned or claimed cleaned.
- Prior S2/S46 incident disclosures, seconds-resolution PID-birth limitation and earlier socket-length baseline failures remain inherited, not investigated/remediated by this task. The native transport trusts controlled same-UID participants, not hostile same-UID clients. Android/old-stock writer compatibility and production rollout remain separate decisions.

## Consolidated blocker / smallest next action

Locate the Prepared stall in the existing same-process reviewed stop/disconnect path before claiming concurrent-restart acceptance. Source observations: `agent_management/stock_runtime.rs::execute_stock_runtime_locked` saves Prepared then calls `runtime.stop` while holding existing authority/Task fences; `cli_app/stock_lifecycle.rs::stop` verifies, interrupts, disconnects and stops the exact owner; `app_server/manager.rs::stop_managed_runtime` joins the existing Bus bridge; `bus_bridge.rs::stop_and_join` unregisters and joins. These are investigation locations, **not a proven deadlock attribution**. The two successful crash recoveries restarted a new Management process, so they do not prove this same-process path. Do not weaken fences or fabricate a generation change to make the test pass. Recommend one bounded continuation to identify this exact phase before any broader lifecycle change.

The final failed-feature bytes are preserved separately in `../artifacts/presentation-job-r1/build-manifest-r2.json`, SHA256 `68306a73074440732ae73de13c02c5626dd1eff08ea9a687755095342fd3f3ab`; old default/feature manifests remain unchanged. Their only Rust delta is inside the existing test-feature-only latch. Final source/doc descendant is reported by Task Service, while both compiled source identities remain explicit above. Final `cargo fmt --all -- --check` and `git diff --check` passed; Python harness syntax checks passed. Frozen R2 binary hashes were rechecked after relocation.

Resources: retained root approximately21.30GB decimal (<20GiB ceiling), filesystem free>350GiB. A force-style removal of a single disposable incremental-cache directory was rejected before execution; **no incremental cache was deleted**. To preserve the final feature bytes without growth, moved the two completed target binaries into the evidence directory and removed only their verified duplicate Cargo hardlink aliases; the unique binaries remain intact (link count1) and can be copied back/rebuilt. No additional builds or cleanup were attempted. All test processes exited/owned namespaces ended; unrelated Human fixtures remain unchanged.

Self-review: complete scoped source delta inspected (typed digest/null validation, v5 preservation/retention, authenticated creation, original input reference/A4, pre/post and pre-CAS fences, independent ACK, pinned bundle and feature-only changes). No source/kernel/Job/TUI/dependency scope expansion. Failed pj01/pj02/pj06/pj07 and passing pj03/pj04/pj05 are retained, not relabeled. Submit as a blocked partial candidate, not PRIVATE INTEGRATION_ALLOWED.
