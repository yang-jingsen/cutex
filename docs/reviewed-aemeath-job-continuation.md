# Authorized Job-focused real-provider continuation

Continuation of immutable94986615; fixture/docs ONLY. Product bytes remain
c69241c/tree76aa102d90f2b60686a51f80a93fec40906782f0, default Cutex908ab036 /
facade3b7b3736, exact native0c425 and Jobf3bc9c8. Full identities remain in
task-root `artifacts/reviewed-aemeath-r1/build-manifest-v2.json`
(SHAe68255843e477012bba56e92d25bb92a5c199fd8999749f85642a303934d6326).
No product/tool policy, direct-only namespace, native or Job changes/rebuilds.

Director authorized one corrected Job-focused Human turn and at most one normal
completion-triggered turn after healthy runtime discovery. Read native owner's
`artifacts/terra-job-visibility-r1/RESULT.md`; no alternate model/provider,
forced-direct tool exposure, prompt loop or paid retry.

## Observed outcome: Job execution and native A4 passed; final response blocked

Fresh owned guest fixture `acceptance-upload/ar1/r3` reused the existing reviewed
ordinary runtime setup, not another Management-create campaign. Full paginated
native inventory reported cutex_job **connected**, with submit, read_output,
query and cancel. Submit schema was nonempty; names/input schemas and digests
were retained before generation. MCP authStatus `unsupported` is the MCP OAuth
status, not failure of this private credential-bound stdio adapter.

Exactly one corrected Human turn permitted tool_search/CodeMode solely to call
this fixed Job, using native callable naming. It prohibited shell/exec_command,
unrelated tools, polling and automatic submit retries. Normal exact-argument
MCP approval stayed in force. The model actually invoked cutex_job.submit once;
Core generated its metadata and the real adapter/daemon accepted it. No manually
constructed grant or model response was substituted.

Independent read-only Job/native-store oracle after cleanup:

- One Job `job_1b6f26f9574e434f8e68ec4803b388fb`, exited0;36 stdout bytes:
  `private-read-success\nreal-job-output`; stderr0.
- Managed permission origin, exact native thread and current runtime occurrence
  match the reviewed receipt; receiver read-only, owned fixture cwd.
- Native rollout has commit/claim/hold. The commit contains original A4 receipt
  `eir1_74e0a3b8ff6df8225df8b3081fa20e18f963710cda5c319a44c1b3bfbe9091a1`.
- Exactly two turn-start observations: Human Job turn and normal completion
  turn. The latter emitted `error`; native persisted hold `request_uncertain`.
- Job completion delivery remains `accepted_pending`. Do NOT equate native A4
  with completed Bus/Job ACK, a successful model turn, or business acceptance.
- No read_output call/final model acknowledgment. No retry permission issued,
  no extra Human turn, no forced wake or fallback. Exact trigger of the native
  error is unclassified: observer retained method/code-null but not nested
  error text. No native patch is justified from that missing observation alone.

Thus the prior “cannot access tool” response is no longer a tool-call blocker
for this actual sample. Full end-to-end output return/final acknowledgment is
still NOT accepted. Stopping on the surfaced error was intentional.

## Observation limits, usage, cleanup

Resolved model is gpt-5.6-terra, reviewed effort low. Static native reference
says CodeModeOnly/search supported; effective final request/tool-mode/provider
namespace capability is not directly observed. No TLS proxy or serialized
request/header/auth dump was added. Actual successful MCP invocation is stronger
than static inference but does not prove all tool-projection modes.

Three usage updates in this continuation: input43635, cached37120, output274
(reasoning106), total43909. Two turns observed; usage updates are not an exact
HTTP request count. Prior r2 total27651 remains separate (combined observed
total71560). Dollar cost and internal transport retries unknown. Native builtin
retry defaults unchanged; no deliberate additional paid generation.

Exact staged/native guest auth files removed by finally and absence verified;
recorded runtime executable absent. Host auth untouched, no syncback. Owned
dummy service/state/history retained as evidence; no production mutation.
Read-only native stderr had0 bytes; bounded native journal/history inspection
did not recover the original error text. No credential-shaped logs disclosed.

Evidence: task-root `artifacts/reviewed-aemeath-r1/evidence/r3-observations.json`,
`r3-independent-oracle.json`, `r3-cleanup.json`; original r1/r2 preserved.
Harness AST syntax and scoped diff check pass. Prior29 executions/28 distinct
focused tests, default build/fmt and native sandbox proofs reused unchanged;
no broad suite/rebuild. Fixture uses actual provider, native/Core, adapter,
daemon, private Cutex stores; only setup/observer is scripted.

## Remaining decision

Return partial candidate with a now-proven actual Job submit/process/output and
native A4. Keep assignment blocked for final output/acknowledgment. Any further
generation needs a new bounded decision; first improve/redact nested native
error observation or use retained exact status evidence, without auto-retry of
the held message. Host rollout, PRH ownership, full profile migration, Windows
and old-writer compatibility remain excluded. Earlier S2/S46/PID/socket risks
unchanged. This is not release authorization.
