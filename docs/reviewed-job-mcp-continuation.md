# Bounded short-path continuation

Continues immutable checkpoint `5f75295877ae40770186bd1c3a9c60def38cf4a9`
after Director authorized short owned fixture names, with no socket geometry or
permission changes. Earlier [result](reviewed-job-mcp-result.md) and all guest
attempts remain available at their original immutable source/artifact identities.

## Results on final default bytes

Cutex SHA256 `94d8392840d5a5a0922189d8d81e22e0d4f2fce75b2139899475d8538b3300ca`;
facade `7f78bd66b124c50d6eddc3799d0b44de481ba0df2a1025604820e851e2f223bf`.
Native0c425/Jobf3bc9c8/host/schema pins unchanged. No native/Job build or edits.

- `d1`: actual Core-configured MCP approval prompt reached, explicitly declined,
  and no Job created. Passed. This is not a completion/A4 scenario despite the
  inherited broad boundary label in its JSON result; its scenario fields are
  authoritative. Earlier fullaccess `a2` execution/output/A4 is reused, not rerun.
- `r1`: local preflight passed (bootstrap/job/runtime socket lengths72/68/100
  bytes). First native owner reached Ready under read-only configuration;
  original launch replay passed. Invalid descriptor version, daemon birth,
  credential path and endpoint requests returned500; nonroot review returned401.
  Field-specific messages were not captured, so these status checks alone are
  not stronger diagnostic proof than their named inputs/source guard tests.
- The same-action changed review and descriptor-omission run requests returned
  errors; the fixture verified the original PID still existed. It did **not**
  save error bodies, so do not claim the omission-specific message rather than
  an earlier CAS refusal was independently established.
- Fresh explicit restart then returned500 `owner_action_required: stock
  confirmation stale`. No restart receipt/claim was committed. Saved state has
  generation1, original launch Ready, no active launch claim, zero model calls,
  no Job state file. Owned cleanup followed. Read-only Job execution, denied
  writes, actual nested helper execution and wrong-cwd tool call were **not
  reached**. The owned daemon exec trace has only its initial exec, not a
  sandbox-helper proof.

Private guest evidence root:
`/home/cutex-linux-test/acceptance-upload/job-launch-r1/{d1,r1}`.
No old a1/a2/decline1 files were overwritten. The new fixture checks canonical
owned root, a nonexistent run directory and complete compact socket byte lengths
before any child/state creation. Test-only tracing is limited to the owned Job
daemon process tree; no global process arguments or secret values are collected.

## New boundary and smallest next decision

The restart refused before destructive stop, so CAS was not bypassed. A concrete
source mechanism exists: `review_stock_runtime_locked` snapshots the full durable
record before bundle/native/profile validation; the root handler then validates
the bundle and Job descriptor again. The original full-record digest is carried
to `run`. The bridge re-registers every30seconds, and registration reconciliation
updates `last_seen_at`/`updated_at`, which are part of that full-record digest.
Expensive validation can therefore return an already-stale confirmation.

This is a source-supported explanation, **not a proved field-level cause for
r1**: the fixture did not persist the fresh review that failed. Do not silently
refresh its token, omit digest fields, slow/disable registration, or retry until
lucky. Proposed bounded next work: preserve returned review and corresponding
current record observations; decide a review-freshness repair that retains exact
original semantic confirmation/CAS, or run the independent read-only oracle
without claiming restart has passed. No implementation of such a repair is in
this checkpoint. New actual attempts paused pending that decision.

## Local checks and remaining acceptance

Added pre-I/O descriptor version/adapter/launcher/coherent-bundle refusal tests
and forbidden env/approval fields. `job_mcp` now5 passed; unchanged stock7 and
bootstrap8 reused (20 distinct selected tests). Formatting/diff checks and
default build verified; source self-review covers only fixture/test/doc delta
since5f752958. No production logic changes in this continuation.

Open: successful reviewed restart, actual readOnly/helper/denied writes,
configured wrong-cwd/thread/metadata and stale-occurrence negatives, and actual
bootstrap Some/defaultNone discovery as itemized in the first checkpoint.
Current evidence does not authorize integration acceptance or deployment.
All actors/credentials/providers are dummy; no real-provider/aemeath smoke,
host PRH/service/profile/backup, Windows, or native migration work. Prior risks
and candidate-only writer restrictions remain unchanged. Owned fixtures cleaned
their child groups; VM remains running.
