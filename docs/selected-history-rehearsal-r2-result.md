# Selected34 private rehearsal R2 — partial, stopped at second runtime failure

No live migration or release acceptance. The authority prerequisite from R1 is
resolved and proven privately. The remaining actual runtime matrix is incomplete.

## Identities and frozen bytes

Base `4de5e88413621046b9da7827ce7f149e9f4786d1`, tree
`a72fc843223b4e5832ceed6fc834b77defc686ff`, accepted product aaca8bd1.
Initial fixture commit `30a589e`; bounded product correction
`6e3b815ca13d8305fb267e30677ee73110e8a96d`, tree
`18cc305a49c340595f451b3e7218a953ae9fc9e3`; second-run fixture `8fdb223`.
Post-run diagnostic/group corrections are explicitly unexecuted.

Default dev artifacts (not release optimization):
`../artifacts/selected-history-r2/build-manifest.json`, SHA256
`726496f7160429e512c3be52470ba4fa44334e528e5a11d5b3e8375a3e891ad3`.
Cutex `279c12be5210c04a861fb9aa393eb2f770bc0b6eb83a9b701a632068319ec23d`;
facade `d3bacc0caabf3f1a92818c0c62b349c06c18470c1b8ed6a7818c6e9d3e96b929`.
Native remains CLI 8cde7956 / server 2eab060b / official U host / schema 3fc00607;
Job remains f7bbe3c / ba1a8d4f. Full hashes and unchanged native manifest are in
the composed manifest. No native or Job edits/builds.

## Plan, materialization, and authority evidence

Fixed selection is still 7f852831: 34, not an activity reselection. Snapshot:
`../rehearsal-r2-db5WwG/plan.json`, SHA256
`25946fa6993e826888181bd5d64c7c8848838e7741ebfba70242f87b721c073d`.
Current nonsecret metadata capture: 2026-09-11T21:57:46.147691Z.
Names/native IDs/projects/configured profile intent match frozen selection.
Model/effort aggregate agrees with the prior assessment; per-agent model/effort,
cwd and configuration bytes are this explicitly dated capture, not falsely
described as an older per-agent model snapshot. Four inherited subjects use the
accepted frozen aemeath default; live general config was not opened because it
may contain service secrets. No current-default drift claim is made.

31 aemeath (4 inherited), 2 octobre, 1 GLM; 33 full-access/never and 1 read-only/never.
30 paginated and 4 legacy; catalog memory enabled for all34. Original parent/fork
metadata is retained in the plan and unmodified frozen bytes. The plan records
the explicit old `cute_alden` -> private `host` execution selection, not an
identity change or K fallback. Offline source intent remains offline.

Materialized 1,151,747,009 bytes once from the existing frozen prefixes; no new
original-history scan or operational snapshot. `materialized/prepared.json` SHA
`1eb8824359540cb8120760a9cc2fc870a60aa63c8fa510f3c805273930d43433`.
An independent process hashed all34 copies against the native prefix manifest:
34 exact matches and single-link files. No writable hardlinks to frozen input.
The output is prepared history material, not a completed deployable host home.

Independent filesystem/process oracle:
`../rehearsal-fs-r2-eFh52w/filesystem-oracle.json`, SHA
`01d4fab69a9545a9ebce16701ca765c124bd11cfb61ba86c04fb753924abeecd`.
Five passes: existing destination; symlink destination; symlink parent;
interruption with preserved bytes and repeat refusal; concurrent creators with
one exclusive winner. A partial destination is never erased/reused. Parent and
new-directory handles anchor writes. This is the private same-UID boundary,
not a hostile same-UID security claim.

Actual supported CLI adoption/default/profile and root durable-import/project
APIs succeeded for all34 plus exactly one separately labelled nonlaunching
vce2026 Director prerequisite. No prerequisite history was copied or resumed.
All35 private registry records remained generation0/offline; the 16 offline
migration subjects were not started. Project membership and import exact replay
passed in both runs. Fresh private project epochs/Task seats are simulated
reconstruction, not preservation of host authority epochs. The scpolya current
membership remains the same project; old audit provenance is not cloned.
Second-run `all34-PASS.json` SHA
`f35f6df609ed58b9b14afd70955e64dc484ecb0c48678cdab9ef56397e7002f8`.

Post-run independent group comparison found 29 normal cwd-derived convenience
groups added by `adopt`; no original groups were removed. This is **not** a
different authoritative project membership. Exact group equality was not passed.
The fixture now calls existing `session groups set` before import and asserts
exact groups (service.rs:100 uses Set without the adoption normalizer).
That correction has source/AST review only, no third actual run.

## Actual runtime coverage and failures

| Boundary | Evidence / status |
|---|---|
| All34 identity/profile/model/permission/project API import | Actual private stores/providers; pass as above, group caveat explicit |
| Fresh/no-overwrite/interrupted destination | Actual independent filesystem subprocess oracle; pass |
| All34 frozen prefix equality | Independent actual hashes; pass |
| cesc-tutor-r1 preserved-history owner | Actual root review/activation/Ready generation1; inherited aemeath, sol/high, read-only/never, reviewed Job descriptor |
| Historical read | Real thread/read returned matching ID and nonempty turns before PTY phase; response bodies deliberately not retained |
| Native catalog | Independent read-only SQLite: same ID, enabled memory, paginated history |
| Original cesc prefix | 1,793,011 bytes unchanged; zero appended bytes |
| TUI label/pink rendering | Reached checked Bon voyage/profile/pink condition; no input/Enter sent |
| Normal TUI exit and terminal restoration | **Failed combined assertion; separate exit code/restoration values were not retained** |
| Explicit octobre, GLM, legacy/full-access historical runtime probes | Not reached; no replacement by synthetic success claims |
| Whole rehearsal / usable final host home | Incomplete; not accepted |

Attempt1 `../history-r2-9aqcro6n`: all34 import passed, first probe rejected
before native spawn because shared `tui.model_availability_nux` was unrecognized.
FAILURE.json SHA `d4f8c19ba9df2c51b23c37931d22776ea05b9488760300b80d44eeeffb8e1aeb`.
The fixed native config/types.rs:713,826 defines these persisted tooltip counts
as model-slug -> u32. The minimal typed correction preserves that field with
bounded keys/count, no generic passthrough or change to model availability
authority. Absent/empty serialization stays byte-equivalent, so legacy reviews
are not reinterpreted. Shared config remains hash-bound; unknown keys still fail.

Attempt2 `../history-r2-q2mudgye`: passed activation/readiness/history read and
render condition, then failed at `pty_probe`'s `exit==0 && restored` assertion.
FAILURE.json SHA `cdb1639f1580a91e779274793e862d4deddcf90421c495b441378706a972b401`.
This is not an observed restart timeout; no restart was attempted. Cause cannot
be assigned to native exit versus terminal restoration from the retained data.
The fixture now records exit code, forced-exit flag, restoration, screen hash and
size (not conversation text) before asserting. This is future instrumentation,
not recovered evidence. No third runtime attempt was made.

Two earlier offline capture assumptions were corrected before runtime: old
backend is cute_alden, and some formal names reside only in the authoritative
Management spec. A postmortem accessor typo (`approval_policy` vs `approval`)
was corrected read-only. These are distinct from the two retained runtime runs.

## Tests, safety, and next decision

Selected-profile unit tests: 6 pass; shared-config/version test: 1 pass; default
build pass; scoped rustfmt and diff checks pass. Deterministic `plan` output
hashed identically twice (`2bf983108d3f3312381b874b408f36b27280db7a43fd66cc6f8a50f2aff81c48`).
Reused accepted native all34 decoder/legacy reader and R3 synthetic auth/status
proof; these do not stand in for the unexecuted historical runtime variants.
Final fixture-only diagnostic changes AST-checked, not runtime-tested.

All runtime work used isolated network/PID namespace, private services/accounts,
empty private cwd mappings and nonsecret skill copies. No turn/start or Job
submission was sent; no new persisted history records appeared in the reached
probe. Native attempted catalog/PS MCP/WebSocket connections, which failed in
the isolated network; do not call this “no attempted connection.” No successful
external provider traffic or paid model acceptance. The native nested-userns
warning remains: no new OS-sandbox guarantee inferred from configuration.
Owned children were terminated through tracked handles/birth checks and the
private PID namespace exited. Histories, synthetic auth and failed evidence
remain; no real credential read/copy/chmod or host/VM/live service mutation.

Owned root approximately 19GiB, free 357788049408 bytes at the resource check.
No cleanup was used to meet the cap. No backup, canonical ref or deployment.

Required next decision: authorize a bounded corrected continuation after the
two-failure limit, with exit/restoration diagnostics and exact-group assertion,
then finish the remaining representative variants. Do not require another
authority redesign: the vce prerequisite path is now demonstrated. Live auth
0775 custody maintenance, active-role coordination, one operational snapshot,
and actual selected migration remain separate. New-writer/history compatibility
and old-snapshot loss caveats remain; old binaries are not a rollback reader.
Four damaged active histories stay quarantined; retired agents never revive;
pj07 remains deferred, not reproduced by this task.
