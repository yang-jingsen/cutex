# Selected projection R3

Private integration evidence only; no migration, real-provider, VM or release acceptance.

## Fixed source and byte inventory

Base `ec57aab72d9f8e447a2d53f67d87d11dd8c17935` / tree `3a580469c500f436c9a0cfbc901890d3714ee9e0`.
Product `aaca8bd1a065b9570782123263fe1f88f52fe50f` / tree `8915812a22cd79ad46fbfd20a1ea55a45026e641`.
Test-only correction `40e3a3f63b29c11e2111de01f24b65181ae4a30b` / tree `2e56d1ecb44f0778080831473826926586d1a9df` does not change compiled behavior.

`../artifacts/selected-projection-r3/build-manifest.json` SHA256 `3bc284978dc2e5cb61c3b1021ca24bd87c3adbe7f338dceeada97d92786098e3` binds default-feature dev Cutex `d3695adb2e66d56a301b81a810e737fa6294a98510bfb13e5ffb183c8aa57376` and facade `c12033f08cc1eed68f4830f61bf1cd746c3fb1eb357473aecf9861240a8b23fe`. Not release optimization. Native accepted CLI `8cde7956` / `f66dd912…`, server `2eab060b` / `15c72a6b…`, official U host `3e85d674…`, schema `3fc00607…`; all four frozen bytes and native manifest were rehashed. Native manifest SHA256 `6727a35e9154ed53f1384e3c3bcc5362097dd4ef6e3c9de7dd93dec1d3556063`. Job remains f7bbe3c / ba1a8d4f, unchanged and not rebuilt.

## Projection contract and preview

The selected profile's exact materialized `custom-status-items.json` is read as bounded nonsecret data. Only two IDs, Static or LaunchProfile sources and Value rendering are supported. Unsupported sources/fields/rendering, missing selected items, duplicates, bad styles or controls reject. The launch label is `StoredAccount.name`, including the account resolved through the actual default-profile selection; this matches legacy `launch/env.rs`, not account/email/thread/cwd inference. All three selected catalog files were narrowly read: configured Bon voyage text, pink `#F6A3C8` bold and profile white/bold are preserved.

Status-line selection/order remains the typed native TUI configuration. Version-1 inert payload, source digest and deterministic destination path are frozen inside the optional selected-profile review field. The path is `reviewed-status-<payload SHA256>.json` beside the selected catalog. Attachment creates it with create-new/0600, or verifies identical existing contents; it never overwrites a tampered/partial file. Such a failure remains explicit. Missing write permission is not bypassed. Catalog changes require a fresh review; profile, text/style, path and selection changes conflict with the frozen projection.

Only the interactive same-owner `stock-attach` path passes `--status-items-file`; direct app-server/bootstrap receives none. Remote attach still omits `--auth-file`. The independent auth owner/bootstrap/restart behavior is unchanged. No environment, command, template or general plugin execution is introduced.

Observed aemeath footer:

```text
Bon voyage ! · aemeath · gpt-5.6-sol max
```

Pink and bold are configured data, not universal replacements. Display data is not model input, identity or account authority.

## Compatibility and scope

The optional status field is omitted when absent, retaining R2/legacy review serialization; incompatible old readers reject the new field. New payload version must equal 1. Old reviewed records and native histories are untouched. New launches require the exact accepted CLI bundle; the old CLI is not a fallback and old markers are not rewritten.

R2 model/effort, permission/approval, typed safety/memory/compaction/catalog/skills/trust, Job-descriptor and auth custody rules remain. GLM's enabled but missing sample@debug remains missing, not installed. Real 0775 auth ancestors still need separately authorized preparation; this task did not chmod/read/copy real auth. Four damaged histories remain quarantined; offline subjects remain offline. This is not the later original-history/config materializer or 34-subject rehearsal.

## Focused checks and retained failures

- Default bins check/build passed; scoped rustfmt/diff and complete task source self-review passed.
- Selected profile/status: 8 pass. Stock: 8 pass, manifest-specific ignored test separately run and passed. Runtime-review: 3 pass. Job-descriptor guards: 5 pass, 1 artifact-specific test omitted; no Job execution claimed.
- Actual isolated component fixture `../selected-r2-2vtmzm2r`: 1 pass, 11.06s. Two dummy accounts with same native home, actual native account/read, same-account inode refresh/substitution negatives, inherited selected status, GLM per-child loopback bearer oracle. The GLM child oracle is not a native real-provider request.
- Unit failures retained in `selected-r3-status-tests.log`, `-2.log`: absent tempfile dependency fixed using stdlib; newly owned test file mode corrected; serde unit-variant unknown-field acceptance fixed using empty struct variants. Subsequent status tests passed. `selected-r3-final-selected-tests.log` retained old CLI-vs-server assertion failure; test corrected to the unchanged server provenance, then all 8 passed.
- First composed attempt `../selected-r3-wcx5ohtl`: actual neutral create/adopt/root activation and Ready, inherited aemeath correct label/pink PTY, normal exit/terminal restoration, explicit restart same ID at generation 2 all passed. Then provider refused octobre create with `active_agent_collision`; fixture incorrectly reused Director cwd `/p`. `provider.rs:2391` checks active formal name OR cwd collision. Only one durable session existed; no duplicate create occurred. Corrected fixture uses distinct private `/p/octobre` and `/p/GLM`, frozen trust before review. No provider protection changed.

## Final composed result

Second/final attempt `../selected-r3-ghdragta` **passed** on the frozen default bytes. `PASS.json` SHA256 `7cb3066e1820f87b8a3410041719db3a4f4f7e591f748a06773b319379e8ba04`. First failure remains unchanged; no third run occurred.

| Path | Actual result |
|---|---|
| Inherited aemeath | Neutral persistent native start, supported durable adoption, root review/activation, registration Ready; read-only/never, sol/max; same-owner TUI correct aemeath label/pink, normal exit and terminal restoration |
| Explicit restart | Fresh reviewed restart before fixture project-seat assignment, same durable/native ID, generation 2 Ready; not a protected Director restart or arbitrary crash guarantee |
| Explicit octobre | Root reviewed bootstrap intent consumed by authenticated project Director Management create; one new native/durable owner Ready, full-access/never and sol/max; correct octobre label/pink, normal attached TUI exit |
| Explicit GLM | Same actual managed bootstrap/registration path using dummy independent API file and secret spawn mechanism; full-access/never and glm-5.3/max; correct GLM label/pink, normal attached TUI exit |
| Independent history read | Exactly 3 JSONL files / 3 unique native IDs, each only session_meta; zero user/agent/task-start generation events; no Bon voyage display text in history |

The actual ANSI captures `cli-aemeath.ansi`, `cli-octobre.ansi`, `cli-GLM.ansi` were inspected, not inferred solely from a fixture success string. Besides the aemeath preview above, they show:

```text
Bon voyage ! · octobre · gpt-5.6-sol max
Bon voyage ! · GLM · glm-5.3 max
```

Private fixture auth is synthetic. No turn/start, Job submission, model input or paid request was performed. GLM's test catalog uses a complete pinned native metadata entry with an explicit synthetic glm-5.3/max identity; it is not a real GLM service/catalog-entitlement test. The R2 per-child loopback bearer oracle is reused, while this run additionally proves actual native GLM bootstrap and remote CLI usability without passing an auth-file to the remote TUI. Three threads share one native home; cross-account migration of the same existing thread is **not** claimed.

Startup logs retain a network-isolated ChatGPT catalog-refresh connection failure, a bubblewrap user-namespace warning and a Cutex activity-ingress warning. They did not prevent the measured Ready/attach results. `PASS.json`'s compact phrase “no provider request” must be read as **no model generation or successful external provider call**, not absence of attempted startup catalog networking. No real credentials or external route existed. No shell/sandbox enforcement test ran here; warnings are not a sandbox pass or authority to loosen permissions. Native auth/legacy-history and prior Job/sandbox proofs are reused only at unchanged boundaries.

All direct children were stopped by tracked handles; native owner cleanup checked PID birth/executable, and the private PID/network namespace exited. Histories, failed attempts and synthetic 0600 auth evidence are retained; no Human fixture was cleaned. Final root usage 18 GiB, filesystem free 335 GiB. No source outside Cutex, no host credential/profile/permissions/service/PRH changes, no VM, backup, migration or deployment.

Outcome: selected-profile **private integration candidate**. The custom-status dependency is resolved for this exact accepted native bundle. Next is separately authorized preserved-history/config materialization rehearsal and real auth-directory custody preparation, not another presentation feature or implicit live cutover. Original 34 profiles' settings are represented by the typed projection; this is not an execution test of all 34 original configurations. Old writers/old markers cannot be used as a downgrade path. Prior S2/S46 and pj07 disclosures remain unchanged; two normal private restarts here do not explain or retire the earlier pj07 risk.
